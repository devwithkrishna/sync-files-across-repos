[project]
name = "sync-files-across-repos"
version = "0.1.0"
description = "An action to sync files to multiple repos"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv==1.2.2",
    "pyyaml==6.0.3",
    "requests==2.34.2"
]
